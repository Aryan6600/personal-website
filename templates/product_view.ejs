<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= product.name %> | Product Detail
    </title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            max-width: 1000px;
            margin: 0 auto;
            background-color: #333;
            padding: 20px;
            display: flex;
            flex-direction: column;
            /* Added for relative positioning of the back button */
            position: relative;
        }

        /* --- NEW BACK BUTTON STYLES --- */
        .back-button {
            margin-right: auto;
            width: max-content;
            text-decoration: none;
            color: #Fff;
            font-size: 1em;
            padding: 8px 15px;
            border-radius: 4px;
            background-color: #007bff;
            display: flex;
            align-items: center;
            transition: background-color 0.2s;
        }

        /* Adjusted positioning for main content to avoid overlap */
        h1 {
            margin-top: 40px;
        }

        /* --- EXISTING STYLES --- */
        .product-container {
            display: flex;
            gap: 30px;
            margin-top: 20px;
        }

        .image-section {
            flex: 1;
        }

        .info-section {
            flex: 1;
            padding-right: 20px;
            color: hsl(0, 0%, 80%);
        }

        .product-image {
            width: 100%;
            max-height: 500px;
            object-fit: cover;
            border-radius: 8px;
        }

        .price {
            font-size: 2.5em;
            color: white;
            font-weight: bold;
            margin: 10px 0;
        }

        .stock-status {
            font-size: 1em;
            margin-bottom: 20px;
            font-weight: bold;
            color: <%=product.stock > 0 ? 'green': 'red' %>;
        }

        .buy-button {
            padding: 10px 20px;
            font-size: 1.2em;
            background-color: #007bff;
            border: none;
            border-radius: 4px;
            color: white;
            cursor: pointer;
        }

        .buy-button:disabled {
            background-color: #ccc;
            cursor: not-allowed;
        }

        .features-list {
            list-style-type: disc;
            margin-left: 20px;
        }

        .title {
            color: white;
        }

        .details {
            color: hsl(0, 0%, 80%);
        }

        /* Mobile Adjustments */
        @media (max-width: 768px) {
            .back-button {
                top: 10px;
                left: 10px;
                padding: 5px 10px;
            }

            .product-container {
                flex-direction: column;
            }

            .info-section {
                padding-right: 0;
            }

            .buy-button {
                width: 100%;
                box-sizing: border-box;
                text-align: center;
            }

            h1 {
                margin-top: 20px;
            }

            /* Less top margin on mobile */
        }
    </style>
</head>

<body>
    <a href="/store" class="back-button">
        &larr; Back
    </a>

    <h1 class="title">
        <%= product.title %>
    </h1>
    <ul style="color: #666; font-size: 0.9em; display: flex;width: 100%;flex-wrap: wrap;list-style: none;gap: 1em;padding: 0;">
        <% product.tags.forEach(function(tag) { %>
            <li>
                <a href="#" style="border-radius: 10px;text-decoration: none;color: hsl(0, 0%, 80%);background-color: hsl(0, 0%, 30%);padding: 10px 20px;">
                    <%= tag %>
                </a>
            </li>
            <% }); %>
    </ul>
    <hr>

    <div class="product-container">
        <div class="image-section">
            <img src="<%= product.image %>" alt="<%= product.name %>" class="product-image">
        </div>

        <div class="info-section">
            <p style="font-size: 1.2em; font-style: italic;">
                <%= product.short_desc %>
            </p>
            <%if(product.price){%>
                <p class="price">$<%= product.price.toFixed(2) %>
                </p>
                <%}%>
                    <p class="stock-status">
                        <%= stockStatus %>
                    </p>

                    <a href="<%=product.target_link%>" style="display: inline-block;text-decoration: none;" class="buy-button" <%=product.stock <=0 ? 'disabled' : '' %>>
                        Get
                    </a>

                    <!-- <h3 style="margin-top: 30px;">Key Features</h3>
                    <ul class="features-list">

                        <% product.features && product.features.forEach(function(feature) { %>
                            <li>
                                <%= feature %>
                            </li>
                            <% }); %>
                    </ul> -->
        </div>
    </div>

    <section style="margin-top: 40px;" class="details">
        <h2>Product Details</h2>
        <p>
            <%- product.description %>
        </p>
        <p style="font-size: 0.9em; color: #999;">Customer Rating: <%= product.rating %></p>
    </section>
</body>

</html>